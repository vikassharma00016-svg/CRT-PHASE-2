if-else
  \\check wheather a person is eligible for vote#include<iostream>
using namespace std;
int main(){
    int age;
    cout<<"enter the age"<<endl;
    cin>>age;
    if(age>=18){
        cout<<"eligible for vote"<<endl;
    }
    else{
        cout<<"not eligible for vote";
    }
    return 0;
}
 \\ check the person is eligible for vote and if age is > 50 print senior citizen
#include<iostream>
using namespace std;

int main(){
    int age;
    cout << "Enter the age" << endl;
    cin >> age;

    if(age >= 18){
        if(age >= 50){
            cout << "Senior citizen and eligible for vote";
        } else {
            cout << "Eligible for vote";
        }
    } else {
        if(age < 0){
            cout << "Enter a valid age";
        } else {
            cout << "Not eligible for vote";
        }
    }
\\ check two number and print the largest using ternery operator
#include<iostream>
using namespace std;
int main(){
    int a;
    int b;
    int max;
    cout<<"enter the value in a" <<endl;
    cin>>a;
    cout<<"enter the value in b"<<endl;
    cin>>b;
    max=(a>b)?a:b;
    cout<<"the large value is"<<max;
    return 0;
}
    return 0;
}
